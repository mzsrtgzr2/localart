<h1>{{ name }}
{% if profile_img %}
  <img src="{{ profile_img }}" alt="Profile image" style="width:40px; height:40px; border-radius:50%; margin-left:12px; vertical-align:middle; border:1px solid #ccc;" />
{% endif %}
</h1>

**Genre:** {{ genre }}

**City:** {{ city }}

**Biography:**
{{ biography }}

{% if images %}
## Showcase
<div style="display: flex; flex-wrap: wrap; gap: 16px;">
{% for img in images %}
  <div style="text-align:center; margin-bottom:16px;">
    <a href="{{ base_url }}/artists/{{ slug }}/image/{{ img.file | urlencode | replace('.png', '') | replace('.jpg', '') | replace('.jpeg', '') | replace('.webp', '') }}" style="position:relative; display:inline-block;">
      <img src="{{ base_url }}/assets/artists/{{ slug }}/{{ img.file | urlencode }}" alt="{{ name }}" style="width:200px; height:auto; object-fit:cover; border-radius:8px; display:block; margin:0 auto;" />
      {% if img.sold %}
        <span style="position:absolute; top:8px; left:8px; width:24px; height:24px; background:#e53935; border-radius:50%; display:inline-block; border:2px solid #fff;"></span>
      {% endif %}
    </a>
    <div style="font-size:0.9em; margin-top:4px;">
      <b>{{ img.title }}</b><br>
      Size: {{ img.size }}<br>
      Medium: {{ img.medium }}<br>
      Date: {{ img.date }}<br>
      {% if img.sold %}
      <span style="color:#e53935; font-weight:bold;">SOLD</span><br>
      {% elif img.price %}
      <span style="color:#388e3c; font-weight:bold;">Price: {{ img.price }} ₪</span><br>
      {% endif %}
      {% if img.tags %}
      <div style="margin-top:4px;">
        {% for tag in img.tags %}
          <a href="{{ base_url }}/tags/{{ tag }}" style="background:#eee; color:#333; border-radius:4px; padding:2px 6px; margin-right:4px; font-size:0.85em; text-decoration:none;">{{ tag }}</a>
        {% endfor %}
      </div>
      {% endif %}
    </div>
  </div>
{% endfor %}
</div>
{% endif %}

{% if contact %}
## Contact Information
<ul>
{% for key, value in contact.items() %}
  {% if key in ['instagram', 'facebook', 'twitter', 'website'] %}
    <li><b>{{ key.capitalize() }}:</b> <a href="{{ value }}" target="_blank">{{ value }}</a></li>
  {% else %}
    <li><b>{{ key.capitalize() }}:</b> {{ value }}</li>
  {% endif %}
{% endfor %}
</ul>
{% endif %}
